# Docker 환경용 환경변수
NODE_ENV=production
PORT=3000

# PostgreSQL (Docker Compose에서 제공)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password123
DB_DATABASE=career_lens

# JWT
JWT_SECRET=your-super-secret-jwt-key-for-production-change-this-in-production
