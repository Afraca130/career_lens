# 개발용 Dockerfile
FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# 필요한 패키지 설치
RUN apk add --no-cache dumb-init

# package.json과 package-lock.json 복사
COPY package*.json ./

# 모든 의존성 설치 (개발 의존성 포함)
RUN npm install

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 3000 9229

# 개발 서버 실행 (핫 리로드)
CMD ["dumb-init", "npm", "run", "start:dev"]
